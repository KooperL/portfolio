export interface CaptureRequestPayload {
  canvas_hash: string
  uuid: string
  innerHeight: number
  outerHeight: number
  innerWidth: number
  outerWidth: number
  actualHeight: number
  actualWidth: number
  pixelDepth: number
  platform: string
  cookieEnabled: boolean
  darkMode: boolean
}
