const routes = {
  forumRegister: 'register',
  forumPostCreate: 'post',
  forumPost: 'post/',
  forumUser: 'user/',
  forumLogin: 'login',
  forumRefresh: 'refresh',
  forumLogout: 'logout',
} as const

export const forumPath = "forum"

export {
  routes
}
